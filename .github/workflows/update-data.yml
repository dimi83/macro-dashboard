name: Update financial data

on:
  schedule:
    - cron: '0 22 * * 1-5'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Fetch VIX and write data.json
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
        run: |
          python3 << 'PYEOF'
          import urllib.request, json, datetime, os

          KEY  = os.environ["FRED_API_KEY"]
          BASE = "https://api.stlouisfed.org/fred/series/observations"

          print("Fetching VIX...")
          url = f"{BASE}?series_id=VIXCLS&api_key={KEY}&file_type=json&sort_order=asc&observation_start=2005-01-01"
          with urllib.request.urlopen(url, timeout=30) as r:
              payload = json.loads(r.read())
          vix = [[o["date"][:7], float(o["value"])]
                 for o in payload["observations"] if o["value"] != "."]
          print(f"  {len(vix)} rows, latest: {vix[-1]}")

          out = {
              "updated": datetime.date.today().isoformat(),
              "vix": vix,
          }

          with open("data.json", "w") as f:
              json.dump(out, f, separators=(",", ":"))

          print("data.json written successfully!")
          PYEOF

      - name: Commit data.json
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data.json
          git diff --staged --quiet && echo "No changes to commit" || (git commit -m "chore: update financial data $(date -u +%Y-%m-%d)" && git push)
